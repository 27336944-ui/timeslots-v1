/**
 * 全局类型声明占位文件（受保护 §10）。
 *
 * 用途：放置跨文件的全局类型增强（`declare global { ... }`），
 * 以及扩展 `wx.*` / 第三方模块的类型。**禁止**在这里定义业务类型——
 * 业务类型放 `types/api.ts` 等具体文件。
 *
 * `export {}` 强制本文件成为 ES Module（避免与全局声明冲突）。
 */
export {};
